spring:
  application:
    name: usuario-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

mensaje:
  texto: Configuración desde Config Server para Usuario Service
